CHECKLIST

	localaddress generator { wifi/hotspot port: en0 inet, ethernet port: en5 port}
	subtitles controls
		- inc/dec delay
	restrain unwanted connections
	lock remote screen
	styling and smoothening 
		- style scroll bars
		- style topLeftDisplay function
		- ’ --> this character in .srt is replaced by a question mark in .vtt files
		- GOT7 episodes not rendering sound
		- on first load after starting server thumbnails are sometimes cluttered together

	problems in chrome:
		= desktop chrome still takes time to replace sprites
		- full screen not working